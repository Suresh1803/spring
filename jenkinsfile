pipeline {
  agent any
  tools {
    maven 'maven'
  }
  stages {
    stage ('Build') {
      steps {
        bat 'mvn clean -Dmaven.test.skip package'
      }
    }
    stage ('Deploy') {
      steps {
        script {
          sh 'java -jar target/sprinboot-restapi-0.0.1-SNAPSHOT.jar'
        }
      }
    }
  }
  post { 
        always { 
            cleanWs()
        }
    }
}
